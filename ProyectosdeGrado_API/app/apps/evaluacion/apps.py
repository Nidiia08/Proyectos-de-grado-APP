from django.apps import AppConfig


class EvaluacionConfig(AppConfig):
    """Configuracion de la app evaluacion."""

    default_auto_field = 'django.db.models.AutoField'
    name = 'apps.evaluacion'
    label = 'evaluacion'
