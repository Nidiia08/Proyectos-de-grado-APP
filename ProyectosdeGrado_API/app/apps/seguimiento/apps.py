from django.apps import AppConfig


class SeguimientoConfig(AppConfig):
    """Configuracion de la app seguimiento."""

    default_auto_field = 'django.db.models.AutoField'
    name = 'apps.seguimiento'
    label = 'seguimiento'
